// services/alertService.js
class AlertService {
    static sendAlert(message) {
        console.log("ALERT:", message);  // Alerts now simply print to the console
    }
}

module.exports = AlertService;
